# Copier ce fichier vers .env.local (dev sans Docker) ou .env (Docker Compose)
# et renseigner des valeurs reelles. Ne jamais committer .env / .env.local.

DATABASE_URL="postgresql://postgres:changeme@127.0.0.1:5432/cesizen"
POSTGRES_DB=cesizen
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
